```
function calculaPrecoTotal(quantidade) {
  // Escreva seu código aqui
  let totalCust = quantidade * 1.3
  let promotion = quantidade * 1.0

 if (quantidade < 12) {
        return totalCust 
 
}else{

    return promotion 

} 
```